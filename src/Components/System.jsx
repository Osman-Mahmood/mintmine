import React from 'react'

const System = () => {
  return (
    <div className='p-5 mt-3 pt-3'>
      <h2>Reward System</h2>
      <p>uTokens are the 1:1 representation of the safeguarded native tokens. The u in uTokens stands for "unhackable", "unstealable" and "uncommon".

Only the owner of provided Master Key and the password can move the uTokens. Nobody else can transfer uTokens even if the private keys are publicly exposed.</p>
    </div>
  )
}

export default System
